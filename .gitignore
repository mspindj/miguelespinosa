# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local config — contains accumulated permission grants,
# some with real API keys embedded (e.g. Resend). Never commit.
.claude/settings.local.json

# Confidential job-application materials — repo is public, this stays local-only.
Technical Assessments/
